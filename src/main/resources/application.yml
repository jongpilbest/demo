spring:
  application:
    name: demo

  thymeleaf:
    cache: false
    prefix: file:src/main/resources/templates
    suffix: .html

  datasource:
    url: jdbc:mariadb://localhost:3306/bootstudy
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

server:
  port: 8085

  logging:
    level:
      root: info
      org.springframework: info